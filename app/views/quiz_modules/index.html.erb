<div class="max-w-4xl mx-auto py-10 px-4">
  <h1 class="text-4xl font-bold mb-8"><%= t('modules.title') %></h1>
  
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <% @modules.each do |modulo| %>
      <div class="p-6 bg-bg2 rounded-xl border border-bg3 shadow-md hover:shadow-lg transition duration-200">
        <h2 class="text-xl font-bold mb-2">
          <%= I18n.locale == :en ? modulo.title_en : modulo.title_pt %>
        </h2>
        <p class="text-tx1 mb-4 text-sm font-medium">
          <%= modulo.unlocked ? t('modules.available') : t('modules.locked') %>
        </p>
        <% if modulo.unlocked %>
          <%= link_to t('modules.play'), play_quiz_module_path(modulo.slug, locale: I18n.locale), class: "inline-block bg-accent text-white px-5 py-2.5 rounded-lg hover:brightness-110 font-semibold shadow transition duration-200" %>
        <% else %>
          <span class="inline-block bg-bg3 text-tx2 px-5 py-2.5 rounded-lg cursor-not-allowed font-semibold"><%= t('modules.locked') %></span>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
